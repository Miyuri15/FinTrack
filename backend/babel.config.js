module.exports = {
    presets: [
      '@babel/preset-env',  // Transpile modern JavaScript
      '@babel/preset-typescript',  // Enable TypeScript support
    ],
    plugins: [
      // Any additional Babel plugins can be added here if needed
    ],
  };